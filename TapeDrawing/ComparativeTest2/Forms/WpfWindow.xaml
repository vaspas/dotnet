<Window x:Class="ComparativeTest2.Forms.WpfWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:TapeDrawingWpf="clr-namespace:TapeDrawingWpf;assembly=TapeDrawingWpf" 
        xmlns:l="clr-namespace:ComparativeTest2.Forms"
        Title="WPF" Height="300" Width="300"
        l:ControlBox.HasMaximizeButton="False"
        l:ControlBox.HasMinimizeButton="False">
    
    <!-- Главный грид окна -->
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!-- Элемент, куда будет выводиться изображение -->
        <TapeDrawingWpf:TapeDrawingCanvas Name="drawingCanvas" Grid.Row="1">
        </TapeDrawingWpf:TapeDrawingCanvas>
        
    </Grid>
</Window>
